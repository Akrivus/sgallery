<%= form_with(url: photos_url, html: { class: 'form-inline' }, local: true) do |form| %>
  <%= form.text_field :query, class: 'form-control w-75', placeholder: 'Search photos.' %>
  <%= form.submit 'Search', class: 'btn btn-primary px-0 w-25' %>
<% end %>